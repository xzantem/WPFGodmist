<Window x:Class="GodmistWPF.Dialogs.QuestsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Quests" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Quest Log" 
                   Style="{DynamicResource MaterialDesignHeadline5TextBlock}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Quests Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            
            <!-- Quests List -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,10,0" Padding="10">
                <StackPanel>
                    <TextBlock Text="Available Quests" 
                               Style="{DynamicResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,10"/>
                    
                    <ListBox x:Name="QuestsListBox" 
                             Style="{StaticResource MaterialDesignListBox}"
                             MaxHeight="400"
                             DisplayMemberPath="DisplayName"/>
                </StackPanel>
            </materialDesign:Card>
            
            <!-- Action Buttons -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10">
                <Button Content="Accept Quest" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="AcceptQuestButton_Click"
                        Margin="0,0,0,10"/>
                <Button Content="Abandon Quest" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="AbandonQuestButton_Click"/>
            </StackPanel>
            
            <!-- Quest Info -->
            <materialDesign:Card Grid.Column="2" Margin="10,0,0,0" Padding="10">
                <StackPanel>
                    <TextBlock Text="Quest Information" 
                               Style="{DynamicResource MaterialDesignHeadline6TextBlock}"
                               Margin="0,0,0,10"/>
                    
                    <TextBlock x:Name="QuestInfoText" 
                               Text="Select a quest to see its information." 
                               Style="{DynamicResource MaterialDesignBody2TextBlock}"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button x:Name="CloseButton" 
                    Content="Close" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 