<UserControl x:Class="GodmistWPF.Dialogs.LanguageSelectionDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Width="400">
    <materialDesign:Card>
        <StackPanel Margin="16">
            <TextBlock Text="Select Language" Style="{DynamicResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16"/>
            
            <ListBox x:Name="LanguagesListBox" 
                     Style="{StaticResource MaterialDesignListBox}" 
                     SelectionChanged="Language_SelectionChanged"
                     Margin="0,0,0,16">
                <ListBoxItem Tag="en-US">English (United States)</ListBoxItem>
                <ListBoxItem Tag="pl-PL">Polski (Poland)</ListBoxItem>
            </ListBox>
            
            <Button x:Name="CloseButton" 
                    Content="Close" 
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="CloseButton_Click"
                    HorizontalAlignment="Right"/>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
